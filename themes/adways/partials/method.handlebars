<div id="method_{{name}}" class="method item{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if extended_from}} inherited{{/if}}">

    {{#if abstract}}
        <code class="flag abstract">abstract</code>
    {{/if}}

    {{#if static}}
        <code class="flag static">static</code>
    {{/if}}

    {{#if final}}
        <code class="flag final">final</code>
    {{/if}}

    {{#if chainable}}
        <code class="flag chainable">chainable</code>
    {{/if}}

    {{#if async}}
        <code class="flag async">async</code>
    {{/if}}

    {{#if return}}
        <span class="returns-inline">
          <span class="type">&lt;{{#crossLink returnType}}{{/crossLink}}&gt;</span>
        </span>
    {{/if}}

    <strong class="name">`{{name}}`</strong>( `{{paramsList}} ` )

    {{#if access}}
        <code class="flag {{access}}">{{access}}</code>
    {{/if}}

    {{#if deprecated}}
        <code class="flag deprecated" {{#if deprecationMessage}} title="{{deprecationMessage}}" {{/if}}>deprecated</code>
    {{/if}}

    <div class="meta">
        {{#if overwritten_from}}
            <p>Inherited from
                {{#if overwritten_from/class}}
                    <a href="{{crossLinkRaw overwritten_from/class}}#method_{{overwritten_from/name}}">
                    {{overwritten_from/class}}
                  </a>
                {{/if}}
                {{#if overwritten_from/interface}}
                    <a href="{{crossLinkRaw overwritten_from/interface}}#method_{{overwritten_from/name}}">
                {{overwritten_from/interface}}
              </a>
                {{/if}}
                {{#if overwritten_from/overwritter}}
                    but overwritten by
                    {{#crossLink overwritten_from/overwritter}}{{/crossLink}}
                {{/if}}
                {{#if overwritten_from/implementer}}
                    , implemented by
                    {{#crossLink overwritten_from/implementer}}{{/crossLink}}
                {{/if}}
        {{else}}
            {{#if extended_from}}
                <p>Inherited from
                    <a href="{{crossLinkRaw extended_from}}#method_{{name}}">{{extended_from}}</a>:
            {{else}}
                {{#providedBy}}
                    <p>Provided by the <a href="../modules/{{.}}.html">{{.}}</a> module.</p>
                {{/providedBy}}
                <p>
            {{/if}}
        {{/if}}
        </p>


        {{#if deprecationMessage}}
            <p>Deprecated: {{deprecationMessage}}</p>
        {{/if}}

        {{#if since}}
            <p>Available since {{since}}</p>
        {{/if}}
    </div>

    <div class="description">
        {{{methodDescription}}}
    </div>

    {{#if params}}
        <div class="params">
            <h4>Parameters:</h4>

            <ul class="params-list">
                {{#params}}
                    <li class="param">
                        {{#if optional}}
                            <code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
                            <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>
                            <span class="flag optional" title="This parameter is optional.">optional</span>
                        {{else}}
                            <code class="param-name">{{name}}</code>
                            <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>
                        {{/if}}

                        {{#if multiple}}
                            <span class="flag multiple" title="This argument may occur one or more times.">multiple</span>
                        {{/if}}

                        <div class="param-description">
                            {{{description}}}
                        </div>

                        {{#if props}}
                            <ul class="params-list">
                                {{#props}}
                                    <li class="param">
                                        {{#if optional}}
                                            <code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
                                            <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>
                                            <span class="flag optional" title="This parameter is optional.">optional</span>
                                        {{else}}
                                            <code class="param-name">{{name}}</code>
                                            <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>
                                        {{/if}}

                                        <div class="param-description">
                                            {{{description}}}
                                        </div>

                                        {{#if props}}
                                            <ul class="params-list">
                                                {{#props}}
                                                    <li class="param">
                                                        {{#if optional}}
                                                            <code class="param-name optional">[{{name}}{{#if optdefault}}={{optdefault}}{{/if}}]</code>
                                                            <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>
                                                            <span class="flag optional" title="This parameter is optional.">optional</span>
                                                        {{else}}
                                                            <code class="param-name">{{name}}</code>
                                                            <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>
                                                        {{/if}}

                                                        <div class="param-description">
                                                            {{{description}}}
                                                        </div>
                                                    </li>
                                                {{/props}}
                                            </ul>
                                        {{/if}}
                                    </li>
                                {{/props}}
                            </ul>
                        {{/if}}
                    </li>
                {{/params}}
            </ul>
        </div>
    {{/if}}

    {{#throws}}
        <div class="throws">
            <strong>Throws:</strong>
            {{#if type}}
                <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>:
            {{/if}}
            {{#if description}}
                {{{description}}}
            {{/if}}
        </div>
    {{/throws}}

    {{#fires}}
        <div class="fires">
            <strong>Fires:</strong>
            {{#if type}}
                <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>:
            {{/if}}
            {{#if kind}}
                of kind <span class="type">{{#crossLink kind}}{{/crossLink}}</span>:
            {{/if}}
            {{#if description}}
                {{{description}}}
            {{/if}}
        </div>
    {{/fires}}

    {{#return}}
        <div class="return">
            <strong>Return:</strong>
            {{#if type}}
                <span class="type">&lt;{{#crossLink type}}{{/crossLink}}&gt;</span>:
            {{/if}}
            {{#if description}}
                {{{description}}}
            {{/if}}
        </div>
    {{/return}}

    {{#example}}
        <div class="example">
            <h4>Example:</h4>

            <div class="example-content">
                {{{.}}}
            </div>
        </div>
    {{/example}}
</div>
